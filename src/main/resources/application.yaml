jwt:
  secret: '[a-zA-Z0-9._]^+$Guidelines89797987forAlphabeticalArraNumeralsandOtherSymbo$'

server:
  port: 8081

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/kodsonplus
    password: 0040105715@Kodson12345.com
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false

  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB

mail:
  host: mail.kodsonplusltd.com
  port: 587
  username: info@kodsonplusltd.com
  password: kds23#plus
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true

SERVER_PORT: 8081
VERIFY_EMAIL_HOST: http://localhost:${SERVER_PORT}

